# Junkyard Journey — Ticket 001

## Ticket Title

**Load and Build Level From JSON**

---

## Ticket Summary

Create the first data-driven level-loading prototype for **Junkyard Journey**.

The goal of this ticket is to prove that Unity can load a level definition from a JSON file and generate the basic playable level layout at runtime.

This ticket does **not** include building vehicles, placing parts, physics contraptions, AI generation, or the alien mechanic character yet. It only establishes the foundation for config-driven and AI-generated levels.

---

## Why This Ticket Matters

Junkyard Journey will use programmatically generated levels, including levels generated by GenAI in the future.

To support that, the game must treat levels as **data**, not as hand-built Unity scenes.

This first ticket proves the core pipeline:

```text
level_001.json
    ↓
LevelLoader
    ↓
LevelValidator
    ↓
LevelBuilder
    ↓
Generated Unity level
```

Once this works, future AI-generated level specs can follow the same pipeline.

---

## Goal

Load `level_001.json` from the Unity project and generate:

1. Terrain
2. Start zone
3. Goal zone
4. Basic world bounds validation
5. Console logs confirming the level loaded correctly

---

## Non-Goals

This ticket does **not** include:

- Free-form build mode
- Visible bolts and anchors
- Vehicle construction
- Wheels, engines, rockets, balloons, gears, or pulleys
- Alien mechanic animation
- Win/fail logic
- AI level generation
- Level selection screen
- Saving progress
- Polished art
- Sound effects
- Mobile touch controls

Those come later.

---

## Target Unity Scene

Create or use this scene:

```text
Assets/Scenes/Game.unity
```

The scene should contain:

```text
Game
├── Main Camera
├── GameManager
├── LevelRoot
│   ├── TerrainRoot
│   ├── StartGoalRoot
│   └── ObstacleRoot
```

For this ticket, `ObstacleRoot` can exist but does not need to be used yet.

---

## Required Folder Structure

Create these folders if they do not already exist:

```text
Assets/
  Scripts/
    Core/
    Levels/
    Utilities/
  Data/
    Levels/
  Scenes/
  Prefabs/
    Level/
  Art/
    Placeholder/
```

---

## Files to Create

### C# Scripts

```text
Assets/Scripts/Core/GameManager.cs
Assets/Scripts/Levels/LevelDefinition.cs
Assets/Scripts/Levels/LevelLoader.cs
Assets/Scripts/Levels/LevelValidator.cs
Assets/Scripts/Levels/LevelBuilder.cs
Assets/Scripts/Levels/TerrainBuilder2D.cs
```

### JSON Data

```text
Assets/Data/Levels/level_001.json
```

---

## Level JSON v1 — Minimal Required Fields

For Ticket 1, the level file only needs this subset of the full schema:

```json
{
  "schemaVersion": "1.0",
  "levelId": "junkyard_escape_001",
  "name": "Wake Up, Scraphead",
  "description": "The first escape route from the junk pile. Build something that rolls.",
  "theme": "junkyard",
  "difficulty": 1,

  "worldBounds": {
    "minX": -10,
    "maxX": 90,
    "minY": -25,
    "maxY": 35
  },

  "startZone": {
    "position": { "x": 0, "y": 3 },
    "width": 12,
    "height": 8
  },

  "goal": {
    "type": "reach_zone",
    "position": { "x": 75, "y": 3 },
    "width": 8,
    "height": 8
  },

  "terrain": [
    {
      "id": "main_ground",
      "type": "polyline",
      "points": [
        { "x": -10, "y": 0 },
        { "x": 20, "y": 0 },
        { "x": 40, "y": 2 },
        { "x": 60, "y": 0 },
        { "x": 90, "y": 0 }
      ],
      "surface": "scrap_dirt"
    }
  ]
}
```

---

## Unity Import Note for JSON

Unity does not automatically load arbitrary JSON files from `Assets/Data/Levels` at runtime in a built app unless handled carefully.

For the first prototype, there are two simple options:

### Option A — Recommended for Ticket 1: Use `TextAsset`

Create a public `TextAsset` field on `GameManager` and drag `level_001.json` into it in the Inspector.

This is the simplest and most reliable way to start.

### Option B — Later: Use `StreamingAssets`

Later, move level files to:

```text
Assets/StreamingAssets/Levels/
```

This is better when you want external files, downloadable level packs, or runtime updates.

For now, use **Option A**.

---

## Implementation Plan

## Step 1 — Create `LevelDefinition.cs`

This file defines the C# classes that match the JSON structure.

Suggested structure:

```csharp
using System;
using System.Collections.Generic;

[Serializable]
public class LevelDefinition
{
    public string schemaVersion;
    public string levelId;
    public string name;
    public string description;
    public string theme;
    public int difficulty;

    public WorldBoundsDefinition worldBounds;
    public StartZoneDefinition startZone;
    public GoalDefinition goal;
    public List<TerrainDefinition> terrain;
}

[Serializable]
public class WorldBoundsDefinition
{
    public float minX;
    public float maxX;
    public float minY;
    public float maxY;
}

[Serializable]
public class StartZoneDefinition
{
    public Vector2Definition position;
    public float width;
    public float height;
}

[Serializable]
public class GoalDefinition
{
    public string type;
    public Vector2Definition position;
    public float width;
    public float height;
}

[Serializable]
public class TerrainDefinition
{
    public string id;
    public string type;
    public List<Vector2Definition> points;
    public string surface;
}

[Serializable]
public class Vector2Definition
{
    public float x;
    public float y;

    public UnityEngine.Vector2 ToVector2()
    {
        return new UnityEngine.Vector2(x, y);
    }
}
```

---

## Step 2 — Create `LevelLoader.cs`

Responsible for turning JSON text into a `LevelDefinition` object.

Suggested behavior:

- Accept a `TextAsset`
- Check that it is not null
- Use `JsonUtility.FromJson<LevelDefinition>()`
- Return the parsed level

Suggested structure:

```csharp
using UnityEngine;

public static class LevelLoader
{
    public static LevelDefinition LoadFromTextAsset(TextAsset levelJson)
    {
        if (levelJson == null)
        {
            Debug.LogError("LevelLoader: levelJson is null.");
            return null;
        }

        try
        {
            LevelDefinition level = JsonUtility.FromJson<LevelDefinition>(levelJson.text);

            if (level == null)
            {
                Debug.LogError("LevelLoader: Failed to parse level JSON.");
                return null;
            }

            Debug.Log($"LevelLoader: Loaded level '{level.name}' with id '{level.levelId}'.");
            return level;
        }
        catch (System.Exception ex)
        {
            Debug.LogError($"LevelLoader: Exception while parsing level JSON: {ex.Message}");
            return null;
        }
    }
}
```

---

## Step 3 — Create `LevelValidator.cs`

Responsible for checking that the level has the minimum fields required to build.

For Ticket 1, validation should check:

- Level is not null
- `levelId` exists
- `name` exists
- `worldBounds` exists
- `startZone` exists
- `goal` exists
- `terrain` exists and has at least one terrain definition
- Each terrain has at least 2 points
- Start and goal positions are inside world bounds

Suggested structure:

```csharp
using System.Collections.Generic;
using UnityEngine;

public static class LevelValidator
{
    public static bool Validate(LevelDefinition level, out List<string> errors)
    {
        errors = new List<string>();

        if (level == null)
        {
            errors.Add("Level is null.");
            return false;
        }

        if (string.IsNullOrWhiteSpace(level.levelId))
            errors.Add("Level is missing levelId.");

        if (string.IsNullOrWhiteSpace(level.name))
            errors.Add("Level is missing name.");

        if (level.worldBounds == null)
            errors.Add("Level is missing worldBounds.");

        if (level.startZone == null)
            errors.Add("Level is missing startZone.");

        if (level.goal == null)
            errors.Add("Level is missing goal.");

        if (level.terrain == null || level.terrain.Count == 0)
        {
            errors.Add("Level must contain at least one terrain definition.");
        }
        else
        {
            foreach (TerrainDefinition terrain in level.terrain)
            {
                if (terrain.points == null || terrain.points.Count < 2)
                {
                    errors.Add($"Terrain '{terrain.id}' must contain at least two points.");
                }
            }
        }

        if (level.worldBounds != null && level.startZone != null)
        {
            ValidatePointInsideBounds("startZone", level.startZone.position, level.worldBounds, errors);
        }

        if (level.worldBounds != null && level.goal != null)
        {
            ValidatePointInsideBounds("goal", level.goal.position, level.worldBounds, errors);
        }

        return errors.Count == 0;
    }

    private static void ValidatePointInsideBounds(
        string label,
        Vector2Definition point,
        WorldBoundsDefinition bounds,
        List<string> errors)
    {
        if (point == null)
        {
            errors.Add($"{label} position is missing.");
            return;
        }

        bool inside =
            point.x >= bounds.minX &&
            point.x <= bounds.maxX &&
            point.y >= bounds.minY &&
            point.y <= bounds.maxY;

        if (!inside)
        {
            errors.Add($"{label} position is outside world bounds.");
        }
    }
}
```

---

## Step 4 — Create `TerrainBuilder2D.cs`

Responsible for generating visible and collidable terrain from polyline points.

For Ticket 1, use:

- `LineRenderer` for visual terrain
- `EdgeCollider2D` for collision terrain

Suggested behavior:

- Create one GameObject per terrain definition
- Add `LineRenderer`
- Add `EdgeCollider2D`
- Convert JSON points to `Vector2[]`
- Assign points to both components

Suggested structure:

```csharp
using System.Collections.Generic;
using UnityEngine;

public class TerrainBuilder2D : MonoBehaviour
{
    public Material terrainLineMaterial;
    public float lineWidth = 0.25f;

    public void BuildTerrain(List<TerrainDefinition> terrainDefinitions, Transform terrainRoot)
    {
        if (terrainDefinitions == null)
        {
            Debug.LogWarning("TerrainBuilder2D: No terrain definitions provided.");
            return;
        }

        foreach (TerrainDefinition terrainDefinition in terrainDefinitions)
        {
            BuildSingleTerrain(terrainDefinition, terrainRoot);
        }
    }

    private void BuildSingleTerrain(TerrainDefinition terrainDefinition, Transform terrainRoot)
    {
        if (terrainDefinition == null || terrainDefinition.points == null || terrainDefinition.points.Count < 2)
        {
            Debug.LogWarning("TerrainBuilder2D: Invalid terrain definition skipped.");
            return;
        }

        GameObject terrainObject = new GameObject($"Terrain_{terrainDefinition.id}");
        terrainObject.transform.SetParent(terrainRoot, false);

        Vector2[] points2D = new Vector2[terrainDefinition.points.Count];
        Vector3[] points3D = new Vector3[terrainDefinition.points.Count];

        for (int i = 0; i < terrainDefinition.points.Count; i++)
        {
            Vector2 point = terrainDefinition.points[i].ToVector2();
            points2D[i] = point;
            points3D[i] = new Vector3(point.x, point.y, 0f);
        }

        LineRenderer lineRenderer = terrainObject.AddComponent<LineRenderer>();
        lineRenderer.positionCount = points3D.Length;
        lineRenderer.SetPositions(points3D);
        lineRenderer.useWorldSpace = false;
        lineRenderer.startWidth = lineWidth;
        lineRenderer.endWidth = lineWidth;

        if (terrainLineMaterial != null)
        {
            lineRenderer.material = terrainLineMaterial;
        }

        EdgeCollider2D edgeCollider = terrainObject.AddComponent<EdgeCollider2D>();
        edgeCollider.points = points2D;

        Debug.Log($"TerrainBuilder2D: Built terrain '{terrainDefinition.id}' with {points2D.Length} points.");
    }
}
```

---

## Step 5 — Create `LevelBuilder.cs`

Responsible for building all level objects from a valid `LevelDefinition`.

For Ticket 1, it should:

- Clear old generated level objects
- Build terrain
- Spawn start zone
- Spawn goal zone

Suggested structure:

```csharp
using UnityEngine;

public class LevelBuilder : MonoBehaviour
{
    [Header("Roots")]
    public Transform levelRoot;
    public Transform terrainRoot;
    public Transform startGoalRoot;

    [Header("Builders")]
    public TerrainBuilder2D terrainBuilder;

    [Header("Debug Visuals")]
    public Color startZoneColor = new Color(0f, 1f, 0f, 0.25f);
    public Color goalZoneColor = new Color(1f, 1f, 0f, 0.25f);

    public void BuildLevel(LevelDefinition level)
    {
        if (level == null)
        {
            Debug.LogError("LevelBuilder: Cannot build null level.");
            return;
        }

        ClearRoot(terrainRoot);
        ClearRoot(startGoalRoot);

        terrainBuilder.BuildTerrain(level.terrain, terrainRoot);
        SpawnStartZone(level.startZone);
        SpawnGoalZone(level.goal);

        Debug.Log($"LevelBuilder: Finished building level '{level.name}'.");
    }

    private void SpawnStartZone(StartZoneDefinition startZone)
    {
        GameObject startObject = CreateDebugZone(
            "StartZone",
            startZone.position.ToVector2(),
            startZone.width,
            startZone.height,
            startZoneColor);

        startObject.transform.SetParent(startGoalRoot, false);
    }

    private void SpawnGoalZone(GoalDefinition goal)
    {
        GameObject goalObject = CreateDebugZone(
            "GoalZone",
            goal.position.ToVector2(),
            goal.width,
            goal.height,
            goalZoneColor);

        goalObject.transform.SetParent(startGoalRoot, false);
    }

    private GameObject CreateDebugZone(string name, Vector2 position, float width, float height, Color color)
    {
        GameObject zone = new GameObject(name);
        zone.transform.position = new Vector3(position.x, position.y, 0f);

        SpriteRenderer renderer = zone.AddComponent<SpriteRenderer>();
        renderer.sprite = CreateWhiteSprite();
        renderer.color = color;
        zone.transform.localScale = new Vector3(width, height, 1f);

        BoxCollider2D collider = zone.AddComponent<BoxCollider2D>();
        collider.isTrigger = true;

        return zone;
    }

    private Sprite CreateWhiteSprite()
    {
        Texture2D texture = new Texture2D(1, 1);
        texture.SetPixel(0, 0, Color.white);
        texture.Apply();

        return Sprite.Create(
            texture,
            new Rect(0, 0, 1, 1),
            new Vector2(0.5f, 0.5f),
            1f);
    }

    private void ClearRoot(Transform root)
    {
        if (root == null)
            return;

        for (int i = root.childCount - 1; i >= 0; i--)
        {
            Destroy(root.GetChild(i).gameObject);
        }
    }
}
```

---

## Step 6 — Create `GameManager.cs`

Responsible for coordinating the load, validate, and build flow.

Suggested structure:

```csharp
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    [Header("Level Data")]
    public TextAsset levelJson;

    [Header("Scene References")]
    public LevelBuilder levelBuilder;

    private void Start()
    {
        LoadAndBuildLevel();
    }

    public void LoadAndBuildLevel()
    {
        LevelDefinition level = LevelLoader.LoadFromTextAsset(levelJson);

        if (!LevelValidator.Validate(level, out List<string> validationErrors))
        {
            Debug.LogError("GameManager: Level validation failed.");

            foreach (string error in validationErrors)
            {
                Debug.LogError($" - {error}");
            }

            return;
        }

        Debug.Log("GameManager: Level validation passed.");
        levelBuilder.BuildLevel(level);
    }
}
```

---

## Unity Scene Setup Steps

1. Open `Assets/Scenes/Game.unity`.
2. Create an empty GameObject named `GameManager`.
3. Add the `GameManager.cs` script to it.
4. Create an empty GameObject named `LevelRoot`.
5. Under `LevelRoot`, create:

```text
TerrainRoot
StartGoalRoot
ObstacleRoot
```

6. Add `LevelBuilder.cs` to `LevelRoot` or a separate `LevelBuilder` GameObject.
7. Add `TerrainBuilder2D.cs` to the same object or a child object.
8. Assign references in the Inspector:

```text
GameManager.levelJson      → level_001.json
GameManager.levelBuilder   → LevelBuilder component

LevelBuilder.levelRoot     → LevelRoot
LevelBuilder.terrainRoot   → TerrainRoot
LevelBuilder.startGoalRoot → StartGoalRoot
LevelBuilder.terrainBuilder → TerrainBuilder2D component
```

9. Create a simple material for terrain lines:

```text
Assets/Art/Placeholder/TerrainLineMaterial.mat
```

10. Assign that material to `TerrainBuilder2D.terrainLineMaterial`.

---

## Expected Result

When pressing Play in Unity:

- The console logs that the level was loaded.
- The console logs that validation passed.
- A visible terrain line appears.
- A green-ish transparent start zone appears on the left.
- A yellow-ish transparent goal zone appears on the right.
- Terrain has an `EdgeCollider2D` attached.
- Start and goal zones have trigger colliders.

---

## Acceptance Criteria

This ticket is complete when:

- [ ] `level_001.json` exists in the project.
- [ ] `GameManager` loads the JSON file as a `TextAsset`.
- [ ] `LevelLoader` parses JSON into `LevelDefinition`.
- [ ] `LevelValidator` catches missing or invalid required fields.
- [ ] `LevelBuilder` creates terrain, start zone, and goal zone.
- [ ] Terrain is visible in the scene.
- [ ] Terrain has working 2D collision via `EdgeCollider2D`.
- [ ] Start zone is visible.
- [ ] Goal zone is visible.
- [ ] Editing terrain points in JSON changes the generated terrain.
- [ ] No manual terrain placement is required in Unity.

---

## Manual Test Plan

## Test 1 — Basic Load

1. Open `Game.unity`.
2. Press Play.
3. Confirm terrain, start zone, and goal zone appear.
4. Confirm the console shows no errors.

Expected result:

```text
LevelLoader: Loaded level 'Wake Up, Scraphead' with id 'junkyard_escape_001'.
GameManager: Level validation passed.
TerrainBuilder2D: Built terrain 'main_ground' with 5 points.
LevelBuilder: Finished building level 'Wake Up, Scraphead'.
```

---

## Test 2 — Modify Terrain

1. Open `level_001.json`.
2. Change one terrain point, for example:

```json
{ "x": 40, "y": 8 }
```

3. Save the file.
4. Return to Unity.
5. Press Play.

Expected result:

The terrain shape changes in the scene.

---

## Test 3 — Missing Field Validation

1. Temporarily remove the `goal` object from `level_001.json`.
2. Press Play.

Expected result:

Validation fails and logs an error:

```text
Level is missing goal.
```

Restore the JSON afterward.

---

## Test 4 — Invalid Terrain Validation

1. Temporarily reduce terrain points to one point.
2. Press Play.

Expected result:

Validation fails and logs an error:

```text
Terrain 'main_ground' must contain at least two points.
```

Restore the JSON afterward.

---

## Common Issues

## Issue: JSON does not parse

Check:

- JSON syntax is valid.
- No trailing commas.
- Field names match the C# class names exactly.
- The file is assigned to `GameManager.levelJson`.

---

## Issue: Terrain is invisible

Check:

- `LineRenderer` exists on generated terrain object.
- `lineWidth` is not too small.
- Material is assigned.
- Camera is looking at the terrain.
- Terrain points are inside camera view.

---

## Issue: Start or goal zone is huge or tiny

Check:

- Sprite pixels-per-unit in `CreateWhiteSprite()`.
- Zone scale values.
- Camera size.

For prototype purposes, this is acceptable as long as the zones are visible.

---

## Issue: Terrain collision does not work later

Check:

- `EdgeCollider2D` exists.
- Points are assigned in local space.
- Test object has `Rigidbody2D` and `Collider2D`.

This ticket does not require testing vehicle collision yet.

---

## Design Notes

For this first ticket, keep visuals ugly and simple.

The goal is not to make the level look like a final junkyard. The goal is to prove the data-driven level pipeline.

Later tickets will add:

- SpriteShape terrain
- Junkyard background art
- Scrap piles
- Animated goal gate
- Alien mechanic intro
- Build zone visuals
- Part palette UI

---

## Future Extensions

After this ticket is complete, the next likely tickets are:

1. Create Part Catalog v1
2. Add free-form part placement
3. Add visible anchor points
4. Add bolt snapping
5. Convert build objects into physics objects
6. Add cockpit, frame, and wheel parts
7. Add engine behavior
8. Add win/fail detection
9. Add rocket timers
10. Add AI level generation tooling

---

## Definition of Done

This ticket is done when the project can load a JSON file and generate a visible Unity level at runtime with terrain, start zone, and goal zone.

The final proof should be:

> Change the JSON terrain points, press Play, and see the level change.

